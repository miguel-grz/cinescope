# URL of the deployed backend, no trailing slash. Leave unset for local
# dev — Vite's dev server proxy (see vite.config.js) handles /api locally.
# VITE_API_BASE_URL=https://cinescope-api.onrender.com
